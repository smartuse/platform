To use the notebooks here, first install Julia and Jupyter, following either set of instructions:

http://jupyter.readthedocs.io/en/latest/install.html#installing-jupyter-using-anaconda-and-conda
https://anaconda.org/conda-forge/julia

or

https://github.com/JuliaLang/IJulia.jl#installation

Then install the required packages:

julia -e 'Pkg.add("Query")'
julia -e 'Pkg.add("CSV")'
julia -e 'Pkg.add("JSON")'
julia -e 'Pkg.add("Plots")'

And finally run Jupyter

jupyter notebook
